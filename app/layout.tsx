import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import { SocketProvider } from '@/lib/socket/socket-context'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'PlasticConnect - AI Plastic Recycling Marketplace',
  description: 'Connect plastic waste collectors with recycling brands. Trade smarter, report greener, scale faster.',
  generator: 'v0.app',
  keywords: 'plastic recycling, waste marketplace, B2B trading, compliance certificates',
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        <SocketProvider>
          {children}
        </SocketProvider>
        <Analytics />
      </body>
    </html>
  )
}
